const prompt = require("prompt-sync")();

userInput = prompt("Enter card number: ");

var stringCardNumber(userInput) = ()'
var userInput = 16;
var trimmedString = string.substring(0, length);

let userInput = validate();
console.log(cardvalidation);