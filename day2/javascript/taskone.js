const prompt = required("prompt-sync")();

let scores = [];
console.log("Enter scores);



for(int counter = 1; counter <= 10; counter++){
scores = parselnt(prompt("Enter scores: );
scores.push(scores);
}